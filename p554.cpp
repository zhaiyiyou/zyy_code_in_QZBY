#include <iostream>
using namespace std;

int t, n, a[100005];

int main()
{
    ios::sync_with_stdio(false);

    cin >> t;
    while (t--)
    {
        cin >> n;
        for (int i = 1; i <= n; i++)
        {
            cin >> a[i];
        }
        int dp[n + 5][]
    }
    return 0;
}